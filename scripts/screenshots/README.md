Screenshots captured from the actual built site in the Codex browser on 2026-09-18.
Desktop tutorial: /tutorials/first-site/, viewport 1280x720, light/dark via theme button.
Accessibility: /tutorials/first-site/ in a 1200x960 iframe viewport, Accessibility settings open, Reading tab; full-page capture cropped to the complete panel (560x768 at 506,54). No menu controls synthesized or composited.
Mobile: / in a 390x680 iframe viewport, light/dark via theme button; native browser capture cropped to the iframe bounds (390x680 at 0,0).
Raster conversion and crop only. No generated artwork or recreated UI.

The tutorial Output tab is captured separately from a site built with the exact
content/index.md and mpress.yaml shown in docs/tutorials/first-site.md. It uses
a 1280x720 browser viewport in both themes.

To refresh: build the docs, serve the static output locally, use the real theme
button, and capture the browser. For a constrained viewport, embed the same
local page in a borderless iframe with the dimensions above and crop only its
bounds. Open Accessibility settings in the taller viewport before capturing it.
Keep all of the Reading panel visible, including Bionic reading and Reset settings.
